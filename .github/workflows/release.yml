name: release
on:
  push:
    tags:
      - "*"
  release:
    types: 
      - created
env:
  IMAGE_REPOSITORY: funnyzak/java-nodejs-python-go-etc

jobs:
  docker-release:
    name: Publish Docker images
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Configure docker build context
        uses: docker/setup-buildx-action@v1

      - name: Set reference tags
        id: refs
        run: |
          echo ::set-output name=SOURCE_NAME::${GITHUB_REF#refs/*/}
          echo ::set-output name=SOURCE_BRANCH::${GITHUB_REF#refs/heads/}
          echo ::set-output name=SOURCE_TAG::${GITHUB_REF#refs/tags/}

      - name: Show available tags vars
        run: |
            echo $SOURCE_NAME
            echo $SOURCE_BRANCH
            echo $SOURCE_TAG
        env:
            SOURCE_NAME: ${{ steps.refs.outputs.SOURCE_NAME }}
            SOURCE_BRANCH: ${{ steps.refs.outputs.SOURCE_BRANCH }}
            SOURCE_TAG: ${{ steps.refs.outputs.SOURCE_TAG }}

      - name: Login to Docker
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push docker images
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_REPOSITORY }}:${{ steps.refs.outputs.SOURCE_TAG }}
            ${{ env.IMAGE_REPOSITORY }}:latest
          platforms: linux/amd64,linux/arm64